{
  "name": "netflix-recommendation-with-ml",
  "version": "1.0.0",
  "description": "Netflix recommendation system with TensorFlow.js",
  "type": "module",
  "scripts": {
    "start": "browser-sync -w . --server --files 'index.html, data/*, js/*.js, css/*.css' --port 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}